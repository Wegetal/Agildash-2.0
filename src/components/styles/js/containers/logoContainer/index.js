const container = {
  container: {
    width: "auto",
    height: "10em",
    display: "flex",
    fontSize: "10px",
    alignItems: "center",
    justifyContent: "center"
  }
};

export default container;
